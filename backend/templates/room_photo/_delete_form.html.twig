<form method="post" action="{{ path('app_room_photo_delete', {'id': room_photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room_photo.id) }}">
    <button class="btn">Delete</button>
</form>
