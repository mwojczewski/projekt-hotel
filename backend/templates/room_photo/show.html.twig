{% extends 'base.html.twig' %}

{% block title %}RoomPhoto{% endblock %}

{% block body %}
    <h1>RoomPhoto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room_photo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ room_photo.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_room_photo_index') }}">back to list</a>

    <a href="{{ path('app_room_photo_edit', {'id': room_photo.id}) }}">edit</a>

    {{ include('room_photo/_delete_form.html.twig') }}
{% endblock %}
